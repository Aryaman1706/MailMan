import { Typography } from "@material-ui/core";

const Smtp = () => {
  return (
    <>
      <Typography variant="h5" align="center">
        SMTP Settings
      </Typography>
    </>
  );
};

export default Smtp;
