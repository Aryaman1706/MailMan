import ExcelJS from "exceljs";

import { types as templateTypes } from "../../template";

const generateFile = async (list: templateTypes.Format[]) => {
  try {
    //
  } catch (error) {
    console.error(error);
  }
};
